// function to take in 4 raw bytes and returns them in an IPv4 string format
fn generate_ipv4(a: u8, b: u8, c: u8, d: u8) -> Output {
    // Initialize char array 
    let mut output: [u8; 32] = [0; 32];

    // creating the ipv4 address and saving it to the 'Output' variable
    

}